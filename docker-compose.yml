services:
  postgres:
    image: postgres:16-alpine
    container_name: gemvault-postgres
    environment:
      POSTGRES_USER: gemvault
      POSTGRES_PASSWORD: gemvault
      POSTGRES_DB: gemvault
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gemvault -d gemvault"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: gemvault-backend
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      APP_ENV: development
      DATABASE_URL: postgresql+asyncpg://gemvault:gemvault@postgres:5432/gemvault
      LOG_LEVEL: INFO
      JWT_SECRET: dev-only-secret-replace-in-prod
      JWT_ALGORITHM: HS256
      JWT_EXPIRES_SECONDS: 3600
    ports:
      - "8000:8000"
    profiles:
      - full

volumes:
  postgres-data:
